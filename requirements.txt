# Lower bounds are the tested floors; major caps guard against breaking releases
# (pydantic v2→v3 and numpy 1→2-style transitions; sqlite-vec is still pre-1.0).
fastapi>=0.116
uvicorn[standard]>=0.30
requests>=2.31
pydantic>=2.7,<3
pywebview==6.2.1
numpy>=1.26,<3
sqlite-vec>=0.1.6,<0.2
mcp>=1.0.0

# Native inference engine (INFERENCE_BACKEND=native — see backend/engine/).
# CPU wheels only come from abetlen's release index, not PyPI proper.
#
# CPU build on purpose: the CUDA build is ~900 MB and needs another ~630 MB of
# NVIDIA runtime DLLs, which is dead weight for anyone without an NVIDIA GPU.
# Users who have one enable it from Settings (or CONTEXTVOLT_ENABLE_GPU=1 at
# install time) — backend/engine/cuda_setup.py swaps in the CUDA wheel plus the
# nvidia-*-cu12 runtime packages. Measured ~10x: 10 -> 98 tok/s on an RTX 3050.
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
llama-cpp-python>=0.3.0

# macOS-only: pywebview's WebKit backend needs PyObjC bindings.
# pip skips these on Windows/Linux thanks to the platform marker.
pyobjc-core; sys_platform == "darwin"
pyobjc-framework-Cocoa; sys_platform == "darwin"
pyobjc-framework-WebKit; sys_platform == "darwin"
